# Routes

This project uses **React Router v7** with `<BrowserRouter>` and a central `src/App.tsx` that defines all routes.

Every `.tsx` file in this directory is a page component exported as `export default function PageName()`.

Routes are registered in `src/App.tsx`.
